{
  "name": "home-sorter-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch index.js",
    "db:push": "prisma db push",
    "db:seed": "node seed.js",
    "setup": "prisma generate && prisma db push && node seed.js"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^8.0.7",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "prisma": "^6.5.0"
  }
}
